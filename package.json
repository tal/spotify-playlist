{
  "name": "spotify-thing",
  "version": "1.0.0",
  "license": "MIT",
  "private": true,
  "scripts": {
    "cli": "bun run src/cli-bun.ts",
    "cli:server": "bun run src/cli-bun.ts --server",
    "reauth": "bun run src/reauth.ts",
    "typecheck": "bunx tsc --noEmit",
    "format": "bunx prettier --write .",
    "format:check": "bunx prettier --check .",
    "test": "bun test",
    "test:watch": "bun test --watch"
  },
  "devDependencies": {
    "@types/aws-lambda": "^8.10.162",
    "@types/bun": "^1.3.14",
    "@types/node": "^22.20.1",
    "prettier": "^3.9.6",
    "typescript": "^5.9.3"
  },
  "dependencies": {
    "@aws-sdk/client-dynamodb": "^3.1102.0",
    "@aws-sdk/lib-dynamodb": "^3.1102.0",
    "spotify-web-api-node": "^5.0.2"
  }
}
